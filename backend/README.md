# Backend

Build your app backend here. This directory is your working space.

See `agent-reference/backend/` for a complete reference implementation that demonstrates the rootstock clean architecture pattern with use cases, repository ports, and dependency injection. The architectural guidance in `agent-docs/backend-practices/` and `rootstock-context-v2.md` explains the model in detail.
